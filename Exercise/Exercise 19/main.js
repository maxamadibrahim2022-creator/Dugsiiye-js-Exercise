                
                // **Exercise:**
 // 1. Use the `forEach()` method to log each number in the array `[1, 2, 3, 4, 5]` to the console.


        // Solution
const numbers = [1,2,3,4,5];
numbers.forEach((number)=>{
    console.log("number: " + number);
});